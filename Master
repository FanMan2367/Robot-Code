When Green Flag Pressed
Broadcast setpins
Set speed to 30
Forever()
(If {key {up arrow}or{left arrow}or{back arrow}or{right arrow}} pressed then)
(|set power 7 to 0|)
(|set power 8 to 0|)
(|set power 11 to 0|)
(|set power 12 to 0|)

When Green Flag Pressed
Forever()
(If {key {space} pressed} then)
(|set power7 to 0|)
(|set power8 to 0|)
(|set power11 to 0|)
(|set power12 to 0|)

When [w] pressed
|set power7 to speed|
|set power8 to 0|
|set power11 to 0|
|set power12 to 0|

When [a] pressed
|set power7 to 0|
|set power8 to speed|
|set power11 to 0|
|set power12 to 0|

When [s] pressed
|set power7 to 0|
|set power8 to 0|
|set power11 to speed|
|set power12 to 0

When [d] pressed
|set power7 to 0|
|set power8 to 0|
|set power11 to 0|
|set power12 to speed|

When [q] pressed
|change speed by 5|

When [e] pressed
|change speed by -5|

When Green Flag Pressed
Forever()
If ({speed = 60})
|set speed to 30|

When Green Flag Pressed
Forever()
If ({speed = 10})
|set speed to 30|

When Green Flag Pressed
|set Servo13 to 0|

When key [t] pressed
|change Servo13 by -15|

When key [y] pressed 
|change Servo13 by 15|

When Green Flag Pressed 
If {Servo13 = 105} then
|set Servo13 to 75|

When Green Flag Pressed 
If {Servo13 = -105} then
|set Servo13 to -75|

When Green Flag Pressed 
Forever()
(If {pin13 = 1})
|set power7 to 0|
|set power8 to 0|
|set power11 to 0|
|set power12 to speed|

When Green Flag Pressed 
Forever()
(If {pin16 = 1})
|set power7 to 0|
|set power8 to 0|
|set power11 to speed|
|set power12 to 0|

When Green Flag Pressed
Forever()
(If {pin13 and pin16 = 1} then
|set power7 to 0|
|set power8 to 0|
|set power11 to 0|
|set power12 to 0|
wait 1 second
|set power
